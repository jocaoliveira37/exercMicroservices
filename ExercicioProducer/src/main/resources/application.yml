server:
  port: 8081
  servlet:
    context-path: /post

spring:
  application:
    name: postagem
    rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
  data:
    mongodb:
      host: localhost
      port: 27017
      database: postconsumer

postagem:
  rabbitmq:
    exchange: postagem.exchange
    routingkey: postagem.routingkey